// Let React Native autolink native modules (including react-native-worklets)
// The project previously disabled autolinking for worklets; re-enable it
// so the native WorkletsModule gets registered and initialized at runtime.
module.exports = {};
