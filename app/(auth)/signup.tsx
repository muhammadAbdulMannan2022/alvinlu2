import { ScrollView, Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Signup() {
  return (
    <View>
      <SafeAreaView>
        <ScrollView>
          <Text className="">Auth Index</Text>
        </ScrollView>
      </SafeAreaView>
    </View>
  );
}
